{
  "name": "codem-silverstripe-damn-fine-uploader",
  "version": "1.0.0",
  "description": "Uploader frontend",
  "license": "MIT",
  "author": "James Ellis",
  "engines": {
    "bun": ">=1.4"
  },
  "scripts": {
    "build": "bun run build.mjs",
    "watch": "bun --watch run build.mjs"
  },
  "dependencies": {
    "@uppy/core": "^4",
    "@uppy/dashboard": "^4",
    "@uppy/drag-drop": "^4",
    "@uppy/xhr-upload": "^4"
  }
}
